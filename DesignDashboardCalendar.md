# Design: Dashboard Calendar View

## ภาพรวม

แทนที่ส่วน "รายรับ-รายจ่ายรายวัน" (IncomeExpenseScroll) และกราฟ 2 อัน  
ด้วยปฏิทินเดือนเต็มหน้า แสดงตัวเลขและการแจ้งเตือนภายในแต่ละช่องวัน

ส่วน **สถานะการเงินปัจจุบัน** (FinancialStatus) ยังคงอยู่ด้านบน — ไม่เปลี่ยน

---

## Layout ใหม่ของ Dashboard

```
┌─────────────────────────────────────────────────────────┐
│  สถานะการเงินปัจจุบัน  (FinancialStatus — เหมือนเดิม)    │
│  [ยอดรวม] [เงินสด] [เงินโอน] [หนี้ค้างชำระ] [รอรับเงิน] │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  ◀  พฤษภาคม  2569  ▶                    [วันนี้]         │
├──────┬──────┬──────┬──────┬──────┬──────┬──────────────┤
│  อา  │  จ   │  อ   │  พ   │  พฤ  │  ศ   │  ส            │
├──────┼──────┼──────┼──────┼──────┼──────┼──────────────┤
│      │      │      │      │      │ 9,843│    0          │
│      │      │      │      │      │  -0  │   -0          │
│      │      │      │      │      │  ●   │               │
├──────┼──────┼──────┼──────┼──────┼──────┼──────────────┤
│  ...  (ต่อ)                                              │
└─────────────────────────────────────────────────────────┘
```

---

## Filter Bar (คงไว้เหมือนเดิม)

FilterBar เดิม (`DateRangeFilter`) ยังอยู่ด้านบนปฏิทิน ทำงานร่วมกันแบบ 2-way sync:

| Filter ที่เลือก | ปฏิทินแสดงอะไร |
|---|---|
| **วันนี้** | เปิดเดือนของวันนี้, highlight วันนี้เพิ่มอีกชั้น (ring สีน้ำเงินเข้ม) |
| **เมื่อวาน** | เปิดเดือนของเมื่อวาน, highlight วันเมื่อวาน |
| **เดือน + ปี** (dropdown) | เปิดเดือนนั้น — ปุ่ม ◀/▶ ของปฏิทินเปลี่ยน dropdown ตาม |
| **กำหนดเอง** | เปิดเดือนของ startDate, วันที่อยู่ใน range มีพื้น `bg-blue-50` อ่อนๆ, ◀/▶ เลื่อนดูเดือนอื่นใน range ได้ |

### Calendar Header Navigation

| ส่วน | รายละเอียด |
|---|---|
| ปุ่ม ◀ ▶ | เลื่อนเดือน → sync กับ FilterBar (เปลี่ยน selMonth/selYear, setFilter('month')) |
| ปุ่ม [วันนี้] | กลับเดือนปัจจุบัน + set filter เป็นเดือนนี้ |
| FilterBar | เปลี่ยน filter → ปฏิทิน jump ไปเดือนนั้นอัตโนมัติ |

---

## ช่องวัน (Day Cell)

### โครงสร้างภายในช่อง

```
┌─────────────┐
│           5 │  ← เลขวัน (มุมบนขวา)
│             │
│  9,843      │  ← รายรับ (สีเขียว #10b981) แสดงเฉพาะถ้า > 0
│  -2,100     │  ← รายจ่าย (สีแดง #ef4444) แสดงเฉพาะถ้า > 0
│             │
│  ● ●        │  ← Notification dots (ขอบล่าง)
└─────────────┘
```

### กฎการแสดงตัวเลข
- รายรับ = 0 → ไม่แสดง (ช่องดูโล่ง ไม่รก)
- รายจ่าย = 0 → ไม่แสดง
- ตัวเลขใช้ `toLocaleString()` ไม่มีทศนิยม (เช่น `9,843`)
- ถ้าตัวเลขยาว (>6 หลัก) → ใช้ย่อ เช่น `1.2M`

### กฎ Highlight ช่องวัน (Priority สูง → ต่ำ)

| Priority | สถานการณ์ | Style |
|---|---|---|
| 1 | วันที่มี Note | พื้น `bg-yellow-100` (yellow pastel) — override สีอื่น |
| 2 | วัน Overdue (pending เลยกำหนด) | พื้น `bg-orange-50` |
| 3 | วันปัจจุบัน | ขอบ `border-blue-400` หนา 2px |
| 4 | วันที่อยู่ใน filter range (custom) | พื้น `bg-blue-50` อ่อนๆ |
| 5 | วันนอกเดือนปัจจุบัน | ตัวเลขสีเทาอ่อน, พื้นขาว |

> หากวันที่มี Note **และ** เป็น Overdue → ใช้ yellow เป็น background แต่ยังแสดง dot ส้มตามปกติ

---

## Notification Dots

วงกลมเล็กขนาด 8px เรียงแนวนอนที่ขอบล่างของช่องวัน

### ชนิดและสี

| Dot | สี | เงื่อนไข |
|---|---|---|
| หนี้ค้างชำระถึงกำหนด | ส้ม `#f97316` | `pendingPayments` ที่ `dueDate === วันนั้น` และ `status === 'pending'` |
| รอรับใบกำกับภาษี | ม่วง `#8b5cf6` | `taxInvoices` ที่ `dueDate === วันนั้น` และ `status === 'waiting'` |

### กฎ dot
- 1 dot = 1 รายการ (แสดงสูงสุด 3 dots ต่อวัน หากมากกว่าแสดง `+N`)
- Dot **ไม่หายไปเอง** — หายเมื่อ user เคลียร์บิล (payPending / receiveTaxInvoice)
- วันที่ผ่านมาแล้วแต่ยังค้างอยู่ → dot สีส้มเข้มกว่า + ช่องมีพื้นส้มอ่อน `bg-orange-50` เพื่อบ่งชี้ว่า overdue

---

## Note บนปฏิทิน

### วิธีเพิ่ม / แก้ไข Note
- **คลิกขวา** ที่ช่องวันใดก็ได้ → เปิด Note Popup
- ใช้ได้ทั้งวันที่มีข้อมูลและวันว่าง

### Note Popup

```
┌──────────────────────────────────┐
│  📝 โน้ต — ศุกร์ที่ 1 พ.ค. 2569   │  ← หัวข้อ + ปุ่มปิด ✕
├──────────────────────────────────┤
│  ┌────────────────────────────┐  │
│  │ พิมพ์โน้ตที่นี่...          │  │  ← textarea (4 บรรทัด)
│  │                            │  │
│  └────────────────────────────┘  │
├──────────────────────────────────┤
│  [🗑️ ลบโน้ต]        [บันทึก ✓]  │  ← ลบซ้าย, บันทึกขวา
└──────────────────────────────────┘
```

### กฎ Note Popup
- ถ้าวันนั้นยังไม่มี Note → textarea ว่าง, ปุ่ม "ลบโน้ต" hidden
- ถ้าวันนั้นมี Note อยู่แล้ว → โหลดข้อความเดิมเข้า textarea, ปุ่ม "ลบโน้ต" แสดง
- กด **บันทึก** → บันทึก note, ปิด popup, ช่องวันเปลี่ยนพื้นสีเหลือง
- กด **ลบโน้ต** → ลบ note, ช่องวันกลับสีปกติ
- กด **Escape** หรือ click นอก popup → ปิดโดยไม่บันทึก
- Note รองรับข้อความหลายบรรทัด

### แสดงผลใน Day Cell เมื่อมี Note

```
┌─────────────────┐
│ 📝           5  │  ← icon ดินสอมุมบนซ้าย + เลขวันมุมบนขวา
│                 │
│  9,843          │  ← ตัวเลขปกติ
│  -2,100         │
│                 │  ← พื้นสี bg-yellow-100
│  ● ●            │
└─────────────────┘
```

### Note ปรากฏใน Tooltip ด้วย

```
├──────────────────────────────────┤
│  📝 โน้ต                         │
│  "เช็คสต็อกสินค้าตัวใหม่พรุ่งนี้"  │
└──────────────────────────────────┘
```

### Store สำหรับ Note (ใหม่)

```
useNoteStore  →  localStorage key: 'calendar_notes'
โครงสร้าง: { notes: { '2026-05-01': 'ข้อความ', '2026-05-15': '...' } }
actions: setNote(date, text), deleteNote(date), getNote(date)
```

---

## Hover Tooltip

แสดงเมื่อ hover ค้างไว้ที่ช่องวัน (delay ~300ms เพื่อไม่รบกวน)  
Tooltip ลอยขึ้นมาเหนือช่อง ปรับทิศทางอัตโนมัติถ้าชิดขอบจอ

### โครงสร้าง Tooltip

```
┌──────────────────────────────────┐
│  ศุกร์ที่ 1 พฤษภาคม 2569          │  ← หัวข้อวันที่
├──────────────────────────────────┤
│  รายรับ  9,843 บาท               │  ← หัวข้อสีเขียว
│  ├ เงินสด         5,000          │
│  ├ เงินโอน        3,843          │
│  └ อื่นๆ (บัตร)   1,000          │
├──────────────────────────────────┤
│  รายจ่าย  2,100 บาท              │  ← หัวข้อสีแดง
│  ├ ต้นทุนสินค้า   1,500          │
│  ├ ค่าไฟ            400          │
│  └ ค่าขนส่ง         200          │
├──────────────────────────────────┤
│  แจ้งเตือน                       │  ← แสดงเฉพาะถ้ามี dot
│  ● ค่าเช่า (ครบกำหนดวันนี้)      │  ← สีส้ม
│    ยอด 5,000 บาท                 │
│  ◆ ใบกำกับภาษี #INV-001         │  ← สีม่วง
│    รอรับใบจาก: บริษัท ABC        │
├──────────────────────────────────┤
│  📝 โน้ต                         │  ← แสดงเฉพาะถ้ามี note
│  "เช็คสต็อกสินค้าตัวใหม่..."     │
└──────────────────────────────────┘
```

### กฎ Tooltip
- ไม่มีรายรับ → ซ่อนส่วนรายรับทั้งหมด
- ไม่มีรายจ่าย → ซ่อนส่วนรายจ่ายทั้งหมด
- ไม่มี dot → ซ่อนส่วนแจ้งเตือน
- ถ้าทุกส่วนว่าง (วันโล่ง) → Tooltip ไม่แสดงเลย

---

## การคลิกที่ช่องวัน

คลิกที่ช่องวัน → **navigate ไปหน้า Transactions** โดย pre-filter วันที่นั้น  
(เหมือน FilterBar เลือก "วันนั้น" ให้อัตโนมัติ)

---

## ส่วนที่ถูกลบออก (ไม่แสดงแล้ว)

| ส่วนเดิม | แทนที่ด้วย |
|---|---|
| IncomeExpenseScroll (scroll รายวัน) | ปฏิทิน |
| ChartFiltered (กราฟรายเดือน) | ปฏิทิน (เห็นภาพรวมทั้งเดือนได้แล้ว) |
| TrendChart6M (กราฟ 6 เดือน) | ลบออก หรือย้ายไปหน้า Reports |
| FilterBar | **ยังคงอยู่** — sync กับปฏิทิน 2-way |

---

## ไฟล์ที่ต้องสร้าง / แก้ไข

### สร้างใหม่
```
src/pages/Dashboard/CalendarView.jsx        ← ตัวปฏิทินหลัก + sync กับ FilterBar
src/pages/Dashboard/CalendarDayCell.jsx     ← ช่องวันแต่ละวัน + highlight note (yellow)
src/pages/Dashboard/CalendarTooltip.jsx     ← tooltip เมื่อ hover (รวม note section)
src/pages/Dashboard/CalendarNotePopup.jsx   ← popup เพิ่ม/แก้ไขโน้ต (คลิกขวา)
src/store/useNoteStore.js                   ← store เก็บ note key: 'calendar_notes'
```

### แก้ไข
```
src/pages/Dashboard/index.jsx
  - คง: FilterBar (ด้านบน, sync กับปฏิทิน)
  - คง: FinancialStatus (ด้านบน)
  - ลบ: IncomeExpenseScroll, ChartFiltered, TrendChart6M
  - เพิ่ม: CalendarView (รับ filter/startDate/endDate props จาก index)
```

### ลบได้ (ถ้าไม่ได้ใช้ที่อื่น)
```
src/pages/Dashboard/IncomeExpenseScroll.jsx
src/pages/Dashboard/ChartFiltered.jsx
src/pages/Dashboard/TrendChart6M.jsx
```

---

## Data ที่ CalendarView ต้องดึง

| Store | ข้อมูลที่ใช้ |
|---|---|
| `useTransactionStore` | `transactions` → filter ตาม `date` ของเดือนที่แสดง |
| `usePendingStore` | `pendingPayments` → filter `dueDate` + `status === 'pending'` |
| `usePendingStore` | `taxInvoices` → filter `dueDate` + `status === 'waiting'` |
| `useCategoryStore` | `getCategoryName(id)` → ใช้ใน tooltip expense breakdown |
| `useNoteStore` | `notes` → ทุก key ของเดือนที่แสดง → ใช้ highlight + tooltip + popup |

---

## ข้อควรระวัง

| ประเด็น | แนวทาง |
|---|---|
| หน้าจอเล็ก (< 768px) | ช่องวันเล็กลง แสดงเฉพาะตัวเลข ไม่มี tooltip — ใช้ click แทน |
| ข้อมูลเยอะในวันเดียว | tooltip scroll ได้ (max-height), ตัวเลขในช่องย่อลง |
| ปี พ.ศ. | แปลงปี ค.ศ. → พ.ศ. ด้วย `year + 543` ตลอด |
| วันนอกเดือน | แสดงวัน (ให้ตาราง 7x6 เต็ม) แต่ dim สีและไม่มี hover effect |
| pendingPayments ที่ไม่มี dueDate | บาง record อาจไม่มี `dueDate` → ไม่นำมาแสดง dot |
| Filter "วันนี้" / "เมื่อวาน" | ปฏิทินยังแสดงเต็มเดือน ไม่ใช่แค่วันเดียว — highlight วันนั้นพิเศษ |
| Filter "กำหนดเอง" ข้ามเดือน | ◀/▶ เลื่อนดูแต่ละเดือนได้ วันใน range ยังคง highlight `bg-blue-50` |
| คลิกขวา mobile | mobile ไม่มี right-click → ใช้ long-press (500ms) แทน |
| Note ของวันนอกเดือน | คลิกขวาวันนอกเดือน (dim) → เพิ่ม note ได้ แต่ไม่ navigate ไป Transactions |
